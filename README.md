VocaSprint V4

Die Navigation wurde geändert: Lernen/Importieren/Vokabeln sind jetzt normale Links mit #learn/#import/#words. Dadurch funktioniert die Navigation auch dann, wenn JavaScript kurzzeitig nicht geladen wird. Der Service-Worker nutzt einen neuen Cache-Namen, damit eine alte V3-Version nicht hängen bleibt.
